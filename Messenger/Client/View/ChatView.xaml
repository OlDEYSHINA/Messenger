<UserControl x:Class="Client.View.ChatView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewModels="clr-namespace:Client.ViewModels"
             xmlns:local="clr-namespace:Client.View"
             xmlns:col="clr-namespace:System.Collections;assembly=mscorlib"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance viewModels:ChatViewModel}">
    <Grid Margin="0,0,0,0">
        <ListView SelectedItem="{Binding SelectedUser}" ItemsSource="{Binding UsersStatusesCollection}"  HorizontalAlignment="Left" 
                 Height="391" VerticalAlignment="Top" Width="234" Margin="10,41,0,0" RenderTransformOrigin="0.5,0.5">
            <ListView.View>
                <GridView >
                    <GridViewColumn DisplayMemberBinding="{Binding Path=Name}" Width="100">Имя</GridViewColumn>
                    <GridViewColumn  DisplayMemberBinding="{Binding Path=IsOnline}" >Статус</GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
        <TextBox HorizontalAlignment="Left" Height="19" Margin="320,436,0,0" TextWrapping="Wrap" Text="{Binding Message}" VerticalAlignment="Top" Width="365"/>
        <Button Content="Button" Command="{Binding SendMessage}" HorizontalAlignment="Left" Height="19" Margin="701,436,0,0" VerticalAlignment="Top" Width="89"/>
        <ListView ItemsSource="{Binding ChatMessages}" HorizontalAlignment="Left" Height="377" Margin="320,41,0,0" VerticalAlignment="Top" Width="470">
            <ListView.View>
                <GridView>
                    <GridViewColumn DisplayMemberBinding="{Binding Path=UsernameSource}" Width="100">Имя</GridViewColumn>
                    <GridViewColumn DisplayMemberBinding="{Binding Path=Text}" Width="300">Сообщение</GridViewColumn>
                    <GridViewColumn DisplayMemberBinding="{Binding Path=Time}" Width="80">Время</GridViewColumn>
                </GridView>
            </ListView.View>
            <ListView.Resources>
                <col:ArrayList x:Key="Amogus">

                </col:ArrayList>
            </ListView.Resources>
        </ListView>
        <Menu HorizontalAlignment="Left" Height="26" VerticalAlignment="Top" Width="800" RenderTransformOrigin="0.362,0.923">
            <MenuItem Header="File" Height="26" Width="36">
                <MenuItem Header="Exit"  Command="{Binding MenuExitButton}"></MenuItem>
            </MenuItem>
            <MenuItem Header="Options">
                <MenuItem Header="Settings" ></MenuItem>
            </MenuItem>
            <MenuItem Header="About">
                <MenuItem Header="About"></MenuItem>
            </MenuItem>
        </Menu>

    </Grid>
</UserControl>
